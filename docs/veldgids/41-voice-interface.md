# Block 41 — Voice Interface

Voice is an input/output modality, not an authority channel.

## Pipeline
VOICE INPUT -> TRANSCRIPTION -> USER CONFIRMATION WHEN REQUIRED -> NORMAL ROUTING -> RESPONSE -> OPTIONAL SPEECH OUTPUT

Transcripts inherit the same provenance and privacy rules as typed input.

High-impact or external actions require explicit confirmation through a defined authorization boundary; voice alone does not authorize them.