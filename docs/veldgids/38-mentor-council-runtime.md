# Block 38 — Mentor Council Runtime

The runtime executes an approved routing plan.

## Sequence
QUESTION -> CONTEXT BOUNDARY -> ROUTING -> PARALLEL MENTORS -> COMPARISON -> SYNTHESIS -> SAFETY REVIEW -> RESPONSE

Each invocation receives a bounded context and exact prompt/model versions.

The runtime preserves independent mentor analyses until synthesis. Consensus does not erase dissent.